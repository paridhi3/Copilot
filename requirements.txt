# Core Data Engineering Tools
pandas==1.5.3
numpy==1.24.4
sqlalchemy==1.4.49
psycopg2-binary==2.9.9
pyspark==3.5.0

# Airflow (simplified install for Windows - no extras like celery/dask)
apache-airflow==2.6.3

# dbt (choose appropriate adapter; here using Postgres)
dbt-core==1.7.8
dbt-postgres==1.7.8

# Data Validation
great_expectations==0.17.22

# Testing
pytest==7.4.3

# ML (optional)
scikit-learn==1.3.2
mlflow==2.10.0

# GitHub Copilot-style Productivity (optional helpers)
rich==13.6.0
python-dotenv==1.0.0

# Infrastructure as Code (if doing IaC sessions)
Jinja2==3.1.3

# Ignore known Windows issues with these installs
# You can manually install additional cloud-specific packages if needed:
# e.g., `dbt-snowflake`, `boto3`, `google-cloud-bigquery`
